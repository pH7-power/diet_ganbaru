# ダイエットアプリ 実装ガイド

## 概要

**作成日**：2026-08-21  
**プロジェクト名**：ダイエット トラッカー Web アプリ  
**目標**：78kg → 70kg（2026年10月31日まで）  
**実装言語**：React 18 + Vite  

---

## クイックスタート

### 1. 環境セットアップ（1回のみ）

```bash
cd 個人事業主/ダイエット/app
npm install
```

### 2. 開発サーバーの起動

**ターミナルA：Web アプリ**
```bash
npm run dev
# → http://localhost:5173
```

**ターミナルB：API サーバー（オプション）**
```bash
npm install express cors
node server.js
# → http://localhost:3001
```

### 3. ブラウザで起動

- **PC**：`http://localhost:5173`
- **スマートフォン**（同一WiFi）：`http://<PC-IP>:5173`

---

## 機能一覧

### ✅ 実装済み

| 機能 | 詳細 | タブ |
|---|---|---|
| **体重記録** | 毎朝の体重を入力・保存 | 記録 |
| **運動ログ** | 運動種別・時間から消費カロリーを自動計算 | 記録 |
| **食事記録** | 食事内容と推定カロリーを記録 | 記録 |
| **体重推移グラフ** | 過去の全記録を折れ線グラフで表示 | ホーム |
| **運動記録グラフ** | 日別の消費カロリーを棒グラフで表示 | ホーム |
| **統計情報** | 週間合計・カロリー収支を表示 | ホーム |
| **進捗ダッシュボード** | 目標達成度・残り期間・必要赤字を表示 | 進捗 |
| **ローカルストレージ** | ブラウザに自動保存 | 全タブ |
| **Obsidian 同期** | JSON ファイルへの読み書き対応 | API |

---

## ファイル構成と役割

### `/app/src` フォルダ

| ファイル | 役割 | 重要度 |
|---|---|---|
| `App.jsx` | メインアプリケーション、タブ切り替え・状態管理 | ⭐⭐⭐ |
| `components/Dashboard.jsx` | ホームタブ、グラフ・統計表示 | ⭐⭐⭐ |
| `components/WeightInput.jsx` | 体重入力フォーム | ⭐⭐ |
| `components/ExerciseLog.jsx` | 運動ログフォーム | ⭐⭐ |
| `components/MealLog.jsx` | 食事記録フォーム | ⭐ |
| `components/Stats.jsx` | 進捗表示 | ⭐⭐ |
| `utils/calculations.js` | カロリー計算・統計ロジック | ⭐⭐ |
| `utils/constants.js` | 運動カロリー・定数管理 | ⭐⭐ |
| `utils/obsidian-sync.js` | localStorage と Obsidian API の連携 | ⭐⭐ |

### `/data` フォルダ

| ファイル | 役割 |
|---|---|
| `diet_log.json` | 全データの永続化ファイル |

---

## カスタマイズポイント

### 1. 運動の消費カロリーを調整

**ファイル**：`src/utils/constants.js`

```javascript
export const EXERCISE_CALORIES = {
  running: { name: 'ランニング', calories_per_hour: 600 },
  // ↑ 体重 78kg の場合の値
  // 異なる体重に合わせて調整してください
};
```

**計算式**：
- 消費カロリー = （時間あたりカロリー ÷ 60） × 時間（分）
- 例：ランニング 30分 = （600 ÷ 60） × 30 = 300 kcal

### 2. 目標体重・期限を変更

**ファイル**：
- `/data/diet_log.json` の `metadata` セクション
- `/設計書.md` の「目標設定」セクション

```json
"metadata": {
  "goal_start_weight": 78.0,    // スタート体重
  "goal_target_weight": 70.0,   // 目標体重
  "goal_deadline": "2026-10-31" // 期限
}
```

### 3. 色やテーマをカスタマイズ

**ファイル**：`tailwind.config.js`

```javascript
colors: {
  primary: '#3B82F6',   // 主色（青）
  success: '#10B981',   // 成功色（緑）
  warning: '#F59E0B',   // 注意色（黄）
  danger: '#EF4444',    // 危険色（赤）
}
```

---

## データの永続化フロー

### ローカル（開発環境）

```
入力 → localStorage に保存 → ブラウザ再起動時に復元
```

### 本番（Obsidian 連携）

```
入力 
  ↓
localStorage に保存（即座）
  ↓
API サーバー（server.js）
  ↓
Obsidian vault の diet_log.json に保存
  ↓
Obsidian で自動同期（iCloud/Dropbox）
```

---

## 主要なコンポーネント構成図

```
App.jsx（メイン）
├── WeightInput.jsx
│   └── onSubmit → data 更新 → localStorage + Obsidian API
├── ExerciseLog.jsx
│   └── 消費カロリー自動計算（calculations.js）
├── MealLog.jsx
├── Dashboard.jsx
│   ├── LineChart（Recharts）- 体重推移
│   └── BarChart（Recharts）- 運動記録
└── Stats.jsx
    └── 進捗計算（calculations.js）
```

---

## よくある設定値

### BMI と目標体重の関係

現在：78kg / 173cm
- **BMI** = 78 / (1.73)² = 26.1（過体重）

目標：70kg / 173cm
- **BMI** = 70 / (1.73)² = 23.4（標準体重の上限）

### 1週間の消費カロリー赤字

- 1kg減量 = 7,700 kcal 赤字必要
- 8kg減量 = 61,600 kcal 赤字必要
- 72日間で達成 = 毎日約856 kcal の赤字が必要

**現実的なペース**：
- 毎日1時間のランニング（600 kcal）+ 食事調整（300 kcal削減） = 900 kcal/日

---

## トラブルシューティング

### 問題：グラフが表示されない

**原因**：Recharts が正しくインストールされていない  
**解決**：
```bash
npm install recharts@latest
```

### 問題：データが保存されない

**原因①**：localStorage が無効  
```
DevTools > Application > Local Storage を確認
```

**原因②**：Obsidian API が起動していない  
```bash
node server.js  # API サーバーを起動
```

### 問題：スマートフォンからアクセスできない

**原因**：ファイアウォール、別の WiFi  
**解決**：
```bash
# PC の IP を確認
ipconfig  # Windows
ifconfig  # Mac

# スマートフォンでアクセス
http://<PC-IP>:5173
```

---

## 運用スケジュール

| 日時 | タスク | 所要時間 |
|---|---|---|
| 毎朝 7:00 | 体重記録 | 1分 |
| 日中・夜 | 運動直後に記録 | 30秒/回 |
| 日中・夜 | 食事記録 | 1分/回 |
| 週末（日曜） | ダッシュボードで振り返り | 5分 |
| 月1回（17日） | 財務情報と併せて確認 | 10分 |

---

## 次のステップ（今後の拡張）

### 短期（9月）

- [ ] 実際にデータを入力し始める
- [ ] グラフの見栄えを調整
- [ ] スマートフォンでの操作性をテスト

### 中期（10月）

- [ ] 消費カロリー推定値の精度改善
- [ ] 通知機能（朝に体重記録リマインダー）
- [ ] PDF レポート生成

### 長期（11月〜）

- [ ] Apple Health / Google Fit との連携
- [ ] AI フィードバック（「今週の傾向」など）
- [ ] 本番環境にデプロイ（Vercel）

---

## 技術スタック確認

```bash
npm list --depth=0
```

### 主要ライブラリ

| ライブラリ | 用途 | バージョン |
|---|---|---|
| React | UI フレームワーク | 18.2.0 |
| Vite | ビルドツール | 5.0.8 |
| Recharts | グラフライブラリ | 2.10.3 |
| Tailwind CSS | スタイリング | 3.3.6 |
| Express | API サーバー | 4.18.2 |

---

## 参考ファイル

- 📄 `設計書.md` - 完全な仕様書
- 📖 `README.md` - ユーザーガイド
- 📝 `実装ガイド.md` - このファイル

---

**最後更新**：2026-08-21  
**バージョン**：0.1.0（MVP）
